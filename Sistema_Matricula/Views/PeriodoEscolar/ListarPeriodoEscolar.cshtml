@model IEnumerable<Sistema_Matricula.Models.PeriodoEscolar>

<p>
    <a asp-action="AgregarPeriodoEscolar">Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdPeriodEscolar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdPeriodEscolar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                    @Html.ActionLink("Editar", "EditarPeriodoEscolar", new { id = item.IdPeriodEscolar }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "EliminarPeriodoEscolar", new { id = item.IdPeriodEscolar })
            </td>
        </tr>
}
    </tbody>
</table>
